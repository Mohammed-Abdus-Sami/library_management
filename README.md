# library_management
📚 React-based Library Management System with global state management using useContext &amp; useReducer. Features include book search, add/view books, routing with React Router, local storage persistence using useEffect, and a responsive component-based UI built with modern React Hooks.
